# Host path that contains repositories mounted into the container as /repos
HOST_REPO_ROOT=/home/eepeychev/repos

# Repository folder name under HOST_REPO_ROOT to index
TARGET_REPO_NAME=MCP

# Optional model override for inference helpers
OLLAMA_MODEL=qwen2.5-coder:7b
